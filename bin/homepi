#!/usr/bin/env node --harmony

process.title = 'HomePi';

const path = require('path');
const fs = require('fs');
const src = path.join(path.dirname(fs.realpathSync(__filename)), '../src');

require(src + '/cli')();
